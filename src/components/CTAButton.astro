---
interface Props {
  href: string;
  variant?: 'primary' | 'ghost';
  class?: string;
}

const { href, variant = 'primary', class: className = '' } = Astro.props;
const cls = variant === 'primary' ? 'btn-primary' : 'btn-ghost';
---

<a href={href} class={`${cls} ${className}`}>
  <slot />
</a>
